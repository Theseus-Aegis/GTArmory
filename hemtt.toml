name = "GT Armory"
prefix = "tacgt"
author = "Tyrone, Gilleedoo"
files = [
    "mod.cpp",
    "README.md",
    "tacgt_logo_128.paa",
    "tacgt_logo_64.paa"
]

modname = "gt_armory"
key_name = "{{prefix}}_{{version}}"
authority = "{{prefix}}_{{version}}"

releasebuild = [
    "@zip GTArmory_{{version}}"
]

[header_exts]
version = "{{git \"id 8\"}}"
